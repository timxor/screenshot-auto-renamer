# Vision Framework


## Extract Context from Screenshot using the Vision Framework


We use Vision for the raw "eyes" (perception).

It instantly extracts raw text and object tags from the screenshots image.


### Add Visual Analysis:

Use Vision's `VNClassifyImageRequest` to detect objects/scenes when
there's no text.
